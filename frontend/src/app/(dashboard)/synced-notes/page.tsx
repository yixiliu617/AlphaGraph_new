import SyncedNotesContainer from "./SyncedNotesContainer";

export const metadata = {
  title: "OneNote — AlphaGraph",
};

export default function SyncedNotesPage() {
  return <SyncedNotesContainer />;
}
